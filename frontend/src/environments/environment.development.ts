/**
 * Development configuration (used by `ng serve`).
 *
 * Empty base URL keeps requests relative (/api/...), so proxy.conf.json
 * forwards them to the local backend on :8000 and no CORS is involved.
 */
export const environment = {
  apiBaseUrl: '',
};
